import 'package:flutter/material.dart';

const evtTypeIcon = Icon(
  Icons.abc,
  semanticLabel: "event types",
  size: 25,
);
const evtCatIcon = Icon(
  Icons.category,
  semanticLabel: "event categories",
  size: 20,
);
